name: CI Pipeline - DevSecOps Smart-Uni API

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # ==========================================
  # CI : BUILD, TEST, TRIVY & DOCKER HUB
  # ==========================================
  build-and-secure:
    name: Build & Security Scan
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: smart_residence_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: ci_test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Code checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image (local)
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: smart-uni-api:latest

      - name: Run Unit & Integration Tests
        run: |
          docker run --rm \
            --network host \
            -e SECRET_KEY="ci-test-key-safe-for-github-actions" \
            -e DB_NAME="smart_residence_db" \
            -e DB_USER="postgres" \
            -e DB_PASSWORD="ci_test_password" \
            -e DB_HOST="127.0.0.1" \
            -e DB_PORT="5432" \
            smart-uni-api:latest \
            coverage run manage.py test smart_residence

      - name: Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'smart-uni-api:latest'
          format: 'table'
          exit-code: 1
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          trivyignores: '.trivyignore'

      - name: Login to DockerHub
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push Docker Image to Registry
        if: github.event_name == 'push'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/smart-uni-api:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/smart-uni-api:latest

  # ==========================================
  # TRIGGER CD (smart-uni-infra)
  # ==========================================
  trigger-cd:
    name: Trigger Deployment in Infra Repo
    needs: build-and-secure
    if: github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: Dispatch Event to smart-uni-infra
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INFRA_DISPATCH_TOKEN }}
          repository: Mohamine2/smart-uni-infra
          event-type: deploy-api
          client-payload: '{"sha": "${{ github.sha }}"}'